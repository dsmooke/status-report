const Employee = require("./Employee");
const Role = require("./Role");

class Department {
  constructor(id, name) {
    (this.id = id), (this.name = name);
  }
}

module.exports = Department;

/** CRUD Functions **/

// Create/Add Deparment

// Read/View Departments

// Update Department

// Delete Department
