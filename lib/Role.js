const Employee = require("./Employee");
const Department = require("./Department");

class Role {
  constructor(id, name) {
    (this.id = id), (this.name = name);
  }
}

module.exports = Role;

/** CRUD Functions **/

// Create/Add Role

// Read/View Roles (job titles)

// Update Employee's Role

// Delete Role
